#:kivy 1.0.9

<Root>:
    BoxLayout:
        width: root.width
        height: root.height
        orientation: 'vertical'
        Listen:
            text: 'Text'
            on_release: self.record()
